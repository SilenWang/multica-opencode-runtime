version: "3.8"

services:
  multica-runtime:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: multica-runtime-dev
    env_file:
      - .env
    volumes:
      - ./data/multica_workspaces:/home/ubuntu/multica_workspaces
      - ${HOME}/.local/share/opencode/auth.json:/home/ubuntu/.local/share/opencode/auth.json
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    stdin_open: true
    tty: true
    devices:
      - /dev/dri:/dev/dri
